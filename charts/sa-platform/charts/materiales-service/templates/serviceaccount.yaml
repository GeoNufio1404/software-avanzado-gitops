{{- if .Values.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "sa.serviceAccountName" (dict "root" $ "name" .Chart.Name) }}
  namespace: {{ .Release.Namespace }}
{{- end }}
